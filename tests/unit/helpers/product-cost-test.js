
import { productCost } from 'message-board/helpers/product-cost';
import { module, test } from 'qunit';

module('Unit | Helper | product cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productCost([42]);
  assert.ok(result);
});

