
import { questionPopularity } from 'message-board/helpers/question-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | question popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionPopularity([42]);
  assert.ok(result);
});

