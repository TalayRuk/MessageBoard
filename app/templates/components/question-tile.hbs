<li>
  {{#link-to 'question' question.id}}{{question.topic}} Question: {{question.title}} from {{question.name}}
  {{/link-to}}

  {{#if isQuestionShowing}}
    <a href= {{question.title}} {{action 'questionHide'}}>
    </a>
  {{else}}
      <button {{action 'questionShow'}}>Show Question's detail</button>
  {{/if}}
</li>
